# Public envs (bundled into the app)
# NOTE: These are not secrets. Keep only values safe to expose in clients.

# Supabase
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# OAuth toggles (public)
EXPO_PUBLIC_OAUTH_GOOGLE=false
EXPO_PUBLIC_OAUTH_TWITTER=false
EXPO_PUBLIC_OAUTH_AMAZON=false
EXPO_PUBLIC_OAUTH_LINE=false

# RevenueCat (Public SDK keys)
EXPO_PUBLIC_RC_API_KEY=
EXPO_PUBLIC_RC_IOS_PUBLIC_API_KEY=
EXPO_PUBLIC_RC_ANDROID_PUBLIC_API_KEY=
EXPO_PUBLIC_RC_PENALTY_OFFERING=penalty

# Payments UI presets (optional)
EXPO_PUBLIC_PENALTY_OPTIONS=0,100,500,1000,10000

# Enable E2E screen in non-dev builds (false by default)
EXPO_PUBLIC_ENABLE_E2E=false

